# Terafab Decision Twin — minimal premium GitHub Pages site

This package is a full replacement for the earlier public website layer. It is intentionally concise, clean, mobile-native, PWA-ready, SEO-ready, LLM-crawler-readable, and board-ready.

Primary site folder:

```text
docs/
```

Configured public URL:

```text
https://knowdyn.github.io/terafab-decision-twin/
```

Repository CTA target:

```text
https://github.com/KNOWDYN/terafab-decision-twin
```

The site includes a single visible external CTA: the GitHub icon in the header.

The PWA install action uses the phrase: `Keep this site`.

No restricted source documents are included.
